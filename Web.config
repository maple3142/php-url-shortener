<configuration>
	<system.webServer>
		<rewrite>
			<rules>
				<rule name="rule 1C" stopProcessing="true">
					<match url="^([0-9a-zA-Z]+)$" />
					<action type="Rewrite" url="/go.php?id={R:1}" />
				</rule>
			</rules>
		</rewrite>
	</system.webServer>
</configuration>
